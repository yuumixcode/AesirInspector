

## Codely Structured Memories

### User

### Feedback
- [2026-09-24 22:07:20] Attribute Overview 示例（ExampleSO）拆分与去重规范（2026-09-24 用户拍板，跨会话有效）：①判定重复的唯一标准 = 两个案例**演示的参数集合完全相同**才收敛；A+B 与 A+B+C 不算重复（子集关系保留）②保留 "With xxx" 变体（用于区分简单参数与进阶参数，且分块显示避免单屏内容过多）③Basic 案例针对最常用的参数（最长修改/最常用）④单个 example 过长时建议拆多个 SO。**Why:** 用户明确"只要用于演示的参数不重复即可……目前可以保持 with xxx 的变体"；按此规则审查 15 个多案例特性（35 个 SO）后仅 OnInspectorInit/OnInspectorDispose 两组（参数集均为 Action）需收敛，已合并为单案例并保留独有写法。**How to apply:** 新增/审查示例时先列出每个案例的"演示参数集"再比对；不要因为"主案例已包含变体部分内容"就删变体；不要因内容看起来相似而删除参数集不同的案例。

### Project
- [2026-09-05 16:24:42] [project] AesirInspector assembly scheme (since 0.14.0, 2026-09-05): exactly two assemblies — Runtime `Runestone.AesirInspector` (namespace Runestone.AesirInspector) and Editor `Runestone.AesirInspector.Editor` (namespace Runestone.AesirInspector.Editor); code uses Sirenix APIs directly without #if guards, but ALL asmdefs (主两程序集、Tests、Samples 共 7 个) carry `"defineConstraints": ["ODIN_INSPECTOR"]` — 无 Odin 的项目导入包后程序集整体跳过编译、零报错（用户铁律，2026-09-05 修复于 commit 64ffd88）。**Why:** 0.14.0 merged the former Unity/OdinInspector split assemblies and briefly lost the constraints; pre-0.14 OdinInspector asmdefs were the proven pattern. ODIN_INSPECTOR 全局宏由 Odin 自身维护（写入激活平台的 ScriptingDefineSymbols）。**How to apply:** new code uses only these two namespaces/asmdefs; 新增 asmdef 必须带 ODIN_INSPECTOR 约束；pre-0.14 code needs the migration table in CHANGELOG 0.14.0.

- [2026-09-05 16:42:53] [project] AesirInspector 包导出方案（2026-09-05 统一）：本地与 CI 唯一方案 = .NET 工具 Guardingpearsoftware/public-unity-package-exporter（MIT，固定 commit 91d0bcb，按 SHA fetch）。本地用 Scripts/export-package.sh（首次运行自动装 .NET 8 到 ~/.dotnet 免 sudo，工具缓存在 ~/.cache/aesir-inspector，GitHub HTTPS 不通自动回退 SSH），CI 用 .github/workflows/export-package.yml（dispatch=Artifact，v* 标签=Release，Release Notes 优先取包内 CHANGELOG 版本段落）。Unity 导出脚本 AesirInspectorPackageExporter.cs 已删除（commit 2ff6aab）。**Why:** Unity batchmode 需 Tuanjie 许可证激活且慢；已实测 .NET 工具与 Unity 导出逐字节一致（原 430 条目，删导出脚本后 429）。**How to apply:** 修改包内容无需改脚本/工作流；上游 main 前进致旧 SHA 不可 fetch 时更新两处 TOOL_COMMIT；排除项（Library/**、**/.*、**/Samples~/**、**/Documentation~/**）必须保留——隐藏 ~ 目录无 .meta，工具对缺 .meta 回退逻辑有 bug（写全零 GUID 随机条目）；本地发版 = bump package.json 版本 + 推 v* 标签。

- [2026-09-05 16:44:15] [project] Codely Bridge 与 TJGenerators 已入库（2026-09-05，commit "chore(codely): 内嵌 Codely Bridge 包…"）：Bridge 以 embedded UPM 包形式放在 Packages/cn.tuanjie.codely.bridge（manifest 为普通版本条目，lock 为 source:embedded；embedded 解析优先级高于 manifest file: 引用，CLI 升级时改写 manifest 也不影响）；.codely.packages/ 仍是 CLI 缓存、被忽略。.gitignore 对 .codely-cli/ 采用白名单：默认忽略 .codely-cli/**，仅跟踪 extensions/TJGenerators/（用户拍板整个扩展原样入库，约 115MB，含未安装进工程的 cn.tuanjie.ai.generators Unity 包副本）；.codely-cli/settings.json 不跟踪——用户本机配置可能含个人 API Key（commit 745897b 停止跟踪，克隆机的 TJGenerators MCP 配置由扩展重装流程重建）；UnityInsight 索引、tool-outputs 日志、TMPChineseFont 扩展继续忽略。**Why:** 用户要求 fresh clone 与本地一模一样、Bridge 开箱即用，且 TJGenerators 扩展与 Bridge 强绑定。**How to apply:** 不要把 Packages/cn.tuanjie.codely.bridge、extensions/TJGenerators 当成本地杂物清理或忽略；settings.json 保持忽略、绝不提交（可能含 API Key）；新增 Codely 噪音目录默认已被 .codely-cli/** 覆盖，无需加规则。
- [2026-09-09 17:33:54] ScriptDocGenerator 已独立（Aesir 包 0.15.0，2026-09-09）：脚本文档生成器 + Summary 工具从 Aesir Inspector 包提取到 Assets/ScriptDocGenerator/（普通 Assets 文件夹，非 UPM 包），命名空间 Runestone.ScriptDocGenerator(.Editor)，仍强依赖 Odin（3 个 asmdef 均带 ODIN_INSPECTOR 约束），面板 UI 已由双语特性改为纯 Odin 特性（仅中文）；[Summary]/[ReferenceLinkURL] 特性归属工具。**Why:** 用户拍板缩小 Aesir Inspector 定位——只做 Odin 使用方面的库，doc-gen 不属于该定位。**How to apply:** doc-gen/Summary 相关开发一律在 Assets/ScriptDocGenerator/ 进行，不要回加进 Aesir 包；Aesir 新功能评审时守住"仅 Odin 增强"边界；引用 [Summary] 需 using Runestone.ScriptDocGenerator。
- [2026-09-09 17:33:54] 本机 Unity batchmode 跑 EditMode 测试已验证可用（许可证正常连接）：/Applications/Unity/Hub/Editor/2022.3.62f3c1/Unity.app/Contents/MacOS/Unity -batchmode -nographics -projectPath /Users/yuumix/Projects/Unity/AesirInspector -runTests -testPlatform EditMode -testResults /tmp/results.xml -logFile /tmp/log.txt。**Why:** 全量编译+测试验证无需打开编辑器；与导出脚本避开 Unity batchmode 的决策不冲突（那是导出场景的 Tuanjie 许可问题）。**How to apply:** 需要编译验证/跑测试时优先用该命令（后台运行，数分钟）；仅当 Temp/UnityLockfile 不存在（编辑器未占用）时可用；编译错误在 logFile 中 grep "error CS"。
- [2026-09-09 18:38:43] ScriptDocGenerator 双窗口并存（2026-09-09）：Editor/UIToolkitWindow/ScriptDocGeneratorToolkitWindow 为纯 C# 构建的 EditorWindow（无 UXML/USS），菜单 Tools → Script Doc Generator (UI Toolkit)（order -894），与旧 OdinEditorWindow（-895）并存不替换；用户拍板 MVP 范围（单类型/多类型来源，程序集模式留待迭代）。**Why:** 引入 UI Toolkit 技术栈但保留 Odin 窗口对照，纯 C# 构建避免资产文件依赖。**How to apply:** 窗口后续迭代（程序集模式、多选、UXML 化）在 UIToolkitWindow 目录扩展；不要删除或"合并"Odin 窗口。
- [2026-09-09 18:38:43] 编辑器占用项目时（Temp/UnityLockfile 存在）batchmode 不可用，编译验证改用聚焦法：osascript -e 'tell application "System Events" to set frontmost of process "Unity" to true' 触发自动刷新编译（等 10-15 秒），然后三步验证——(1) stat Library/ScriptAssemblies/<asm>.dll 的 mtime 已更新（不变=尚未编译）；(2) strings <dll> | grep -c <新类型名> 确认类型已编入产物；(3) grep -c 'error CS' ~/Library/Logs/Unity/Editor.log 前后对比无新增（该日志为累计日志，必须前后对比）。**Why:** Unity 后台不自动刷新，聚焦才编译；这是运行中编辑器唯一的自动化验证路径。**How to apply:** 编辑器开着时新增/修改脚本后按此流程验证；对比 mtime 是判断"文件被编译没"的硬证据。
- [2026-09-09 20:28:27] [project] batchmode 跑测试时不要加 -quit：-runTests 自带测试完成后自动退出，多加 -quit 会让 Unity 初始化后直接退出（exit 0 假象），测试一次都不执行、testResults 文件不生成。正确命令见 2026-09-09 记忆（无 -quit）。**Why:** 2026-09-09 实测，加 -quit 后日志出现 "Exiting batchmode successfully now!" 但无任何 TestRun 记录。**How to apply:** 用 -runTests 时绝不带 -quit；验证测试是否真跑了的方法 = testResults XML 文件是否生成 + 日志有 Start running tests 记录。
- [2026-09-09 23:06:15] ScriptDocGenerator 的 SourceScanner/ParsedSourceDoc/ExtractMemberName 是纯 C# 无 Unity API 依赖，调试时可用 ~/.dotnet（9.0.100）建独立 console 工程（/tmp/scanner-repro 模式）：拷贝源码 → sed 去掉 #if UNITY_EDITOR 包裹 → stub 掉 SourceFileAnalyzerUtility.ExtractMemberName（拷贝纯逻辑部分即可）→ 场景断言跑全绿后再回 Unity batchmode 终验。**Why:** 2026-09-09 优化扫描器时用它定位了"花括号在声明行之后导致栈立即弹空"与"特性行分支先压栈后解析导致类型自嵌套"两个系统性 bug，单轮迭代秒级 vs Unity batchmode 每轮约 3-5 分钟。**How to apply:** 修改 SourceScanner/解析键逻辑时先在独立工程跑全场景（含 15 组场景断言），再跑 batchmode EditMode；Unity batchmode 常见坑：runTests 退出码 2=有用例失败（区别于编译错误 1）、CS0136 是 out var 同块作用域重名。
- [2026-09-23 12:08:44] AttributeOverviewUltra Phase 1+2 已完成（2026-09-23，与 Pro 共存、Pro 冻结只修 bug）：Phase 1 = Editor/AttributeOverviewUltra/Core/ 三件套（UltraStateBankSO/UltraPanelDatabase/AttributeOverviewUltraWindow），复用 Pro 全部 70 PanelSO+90 Data+123 Example 类零复制，面板与示例均 CreateInstance 内存实例 + 单银行 UltraStateBank.asset 快照持久化。Phase 2 = 修复 IMGUI 契约 bug（状态变更只允许在 Event.current.type==Layout 的 DrawEditor 开头执行，Repaint 中检测到 NeedsSwap 只 Repaint() 排队下一周期）+ 渲染层优化（图标纹理懒加载+Dispose、表格列宽变化才 Resize（语言切换/建表后必须重置宽度守卫）、GetCachedTextHeight 元组键、高亮/高度按源码变化缓存）、窗口 LanguageChanged→Repaint 配对订阅。**GUITable 重大坑（已修）**：GUITable.Create 对带 ColumnTitle 的列完全丢弃 MinWidth（Odin API 缺陷），窄窗 auto 列被均分压到 ~31px 竖排；修复 = AbstractAttributePanelSO.FixColumnMinWidths(table, params) 建表后手动写入全部单元格，已应用 4 个建表点。**窄窗口防线（已修，2026-09-23 二轮迭代）**：Ultra 窗口 minSize=820×480（=菜单230+边距30+内容区最小560）+ MenuWidth 可拖拽（backing field _menuWidth 真存储 setter，getter 钳制 [100, max(100,width-590)]，ResizableMenuWidth=>true；读写虚属性 override 必须带双访问器否则 CS0546，空 setter 会让拖拽失效=用户感知"菜单固定"）+ AbstractAttributePanelSO.DrawTableWithinContainer(table, tableMinWidth, containerWidth, ref scroll)——容器 < 表最小宽时表格保最小宽 + GUILayout.BeginScrollView(scroll, true, false, ExpandWidth(true), ExpandHeight(false)) 横向滚动兜底（EditorGUILayout.BeginHorizontalScrollView 在 2022.3 不存在；纯布局方案无双重分配问题；滚动状态字段挂 ResolvedStringParameterValue）+ TableCellTextStyle clipping 改 Clip（Overflow 会让不可断行 token 横向压盖列分隔线；基样式 MultiLineLabel wordWrap=true 已确认）。**Why:** 用户拍板示例资产化是无奈之举；银行机制原型(Assets/Prototype/AttributeBankPrototype，可删)已全风险验证。**How to apply:** Phase 3=功能对齐清单核对后移除 Pro（Data 构造器调 .Instance 依赖 Pro 资产，须改造为银行路由）；Pro 窗口未加 minSize/MenuWidth 防线，移除 Pro 后无影响；新增 GUITable 一律建表后 FixColumnMinWidths；Ultra 每帧状态变更遵守 Layout-开头铁律；CustomValueDrawerAttributePanelSO 与 CustomValueDrawerPanelSO 双胞胎产生两个同名菜单项，Phase 3 清理。

- [2026-09-23 11:59:02] AttributeOverviewUltra 示例更新方案（2026-09-23 用户拍板）：底稿 = Odin 官方反编译示例（OdinInspectorReverse/01-Decompiled/Sirenix.OdinInspector.Editor/Sirenix/OdinInspector/Editor/Examples/，即官方 AttributesExampleWindow 的内容源）；落点 = 原地重写 AttributeOverviewPro/UsageExamples/ 的共享示例类（Pro/Ultra 两窗口同步显示，不改类名/基类/挂接）；范围 = 111 个已挂接示例全量、分批提交；10 个孤儿示例（有类无面板不显示）跳过：CustomContextMenuExampleSO(+WithAction)、DisableContextMenuExampleSO、DetailedInfoBoxExampleWithDetails/WithMessage(/WithVisibleIf)、DisableInInlineEditorExampleSO、ShowInInlineEditorExampleSO、HideDuplicateReferenceBoxExampleSO、HideInTablesExampleSO、HideMonoScriptExampleSO、HideReferenceObjectPickerExampleSO。**Why:** 用户要求以官方示例为基准补齐 Ultra 内容；迁移指南中"联网查官方文档验证补充"的角色由仓库内反编译源码取代（更权威）。**How to apply:** 后续新增/维护示例一律先对照反编译 Examples 底稿再叠加本地用法，不删本地案例；分组规范见 AESIR_ATTRIBUTE_MIGRATION_GUIDE.md；ButtonExampleSO.cs 为按新规范重写的黄金样板。
- [2026-09-23 11:59:02] AesirInspector 示例挂接机制硬约束（2026-09-23 梳理）：Data 类 ExamplePreviewItems → XxxExampleSO.Instance 单例子资产；[AesirExample] 携带 FilePath 供 CodePreviewRenderer 提取源码（AttributeOverviewEditorUtility.GetExampleSourceCodeWithoutNamespace），漏标=面板无预览代码；示例类名严禁改名（UltraStateBank 按类名快照恢复、AesirExample 反射按类型匹配）；AesirInspectorReset() 方法体内严禁注释（源码会被展示）。已知坑：文件名≠类名（HideInlineEditorExampleSO.cs 里定义的类是 HideInInlineEditorsExampleSO）；部分文件类声明换行（`public class\n XxxSO`），grep "class X" 需多行匹配；90 个 Data 对 70 个 Panel，多出的 SO 是特殊参数案例（一个 Data 挂多个示例）。
- [2026-09-23 12:23:22] [2026-09-23 14:00] ⚠️ 取代本文件 12:08:44 条目中 Phase 2"窄窗口防线/GUITable 坑修复/渲染层优化"部分（Phase 1 银行机制仍有效）：AttributeOverviewUltra 横向滚动方案已推翻重做（用户拍板"右侧内容整体滚动"语义）。旧方案"每表格独立横向滚动"（DrawTableWithinContainer/FixColumnMinWidths/TableWidthsChanged 列宽跳过/TableCellTextStyle Clip/纹理懒加载+高亮缓存）实测导致布局彻底崩坏——嵌套 ScrollView 的 available>=tableMinWidth 分支与行高跳过使 Layout/Repaint 两遍布局条目不一致（IMGUI 契约破坏），症状=表格溢出压后续标签（游离大字）、块叠块、行高塌陷、嵌套内边距阶梯错位、菜单区被波及。已 git checkout HEAD 回退 8 文件（Abstract/ 6 个 renderer+PanelSO、AttributeOverviewEditorUtility、ResolvedStringParameterValue），仅加回 Ultra 依赖三件套（ExampleSelectionChanged、ExamplePreviewItemsForUltra、ReleaseLanguageSubscription，UltraPanelDatabase.ReleaseAll 调最后一个）。现行方案：Odin 的 OdinEditorWindow.UseScrollView=true（反编译 OdinEditorWindow.cs L133/L685，EditorGUILayout.BeginScrollView 自动双向）天然支持整体滚动——Ultra DrawEditor 里 GUILayout.BeginVertical(GUILayout.MinWidth(560)) 把右侧全部内容声明为整体最小占位：宽窗贴满无滚动条，窄窗出唯一一条整体横向滚动条（滚动整个右侧而非单块）；minSize 放宽 370×320，MenuWidth getter 钳 [100, max(100,width-590)] 窄窗自动收缩菜单。实测窄窗 450px 表格列宽与宽窗逐列一致（60/140/172/172）不再压扁竖排。**How to apply:** ①禁止再给单个表格/块包 ScrollView，横向滚动永远在 DrawEditor 整体层做；②验证横向滚动：反射 OdinEditorWindow 私有字段 scrollPos 塞 10000 重绘后读钳制值（>0=存在，宽窗钳 0）；③Phase 2 性能优化若重做：高亮/高度缓存必须把 CodeAreaWidth 纳入键（曾漏）、纹理懒加载需配 Dispose；④Pro 窗口未加窄窗防线（Odin 默认 ResizableMenuWidth=true，MenuWidth += SlideRect().x 裸累加可拖到 0=菜单归零），是否给 Pro 加整体滚动待用户拍板；⑤Layout-开头状态变更铁律不变。
- [2026-09-23 12:28:04] AesirInspector UsageExamples SO 迁移（Odin 4.0.2.4）关键实证结论（2026-09-23，PropertyTree dump 验证）：(1) Odin 组名/路径禁止含 '.'，@ 表达式组名如 @"G_" + System.DateTime.Now.DayOfWeek 会在 BuildPropertyGroups 报错且按钮不入组——组名用 @ 表达式必须免点（如 @"Group_" + memberField 或 @GetMethod()）；(2) [Title] 不是 PropertyGroupAttribute，不产生组节点，只把标题附着在成员上，可与任意组特性共存不双绘；(3) 成员带两个不相关 ID 的组特性会被双绘（成员进两个组），节区+演示组共存必须用嵌套 ID（如 FoldoutGroup("Sec") + BoxGroup("Sec/Box")），$ 和 @ 在嵌套路径末段可解析（"Sec/$G" 合法）；(4) 无参 [ButtonGroup]/[BoxGroup] 分别落 _DefaultGroup/_DefaultBoxGroup，bare [ButtonGroup(ButtonHeight=40)] 会并入 _DefaultGroup 按钮行，需独立组名；(5) 反编译产物 BoxGroup("g", true, false, 0f)/HorizontalGroup("id", 0f, 0, 0, 0f) 按语义化简为 BoxGroup("g")/HorizontalGroup("id")。验证脚本模式：exec_editor_script + #define UNITY_EDITOR（脚本编译无 UNITY_EDITOR，Odin 的 [Conditional("UNITY_EDITOR")] 特性会被剥离）+ PropertyTree.Create + 递归 dump Info.PropertyType/GetAttribute<PropertyGroupAttribute>().GroupID，用完 tree.Dispose()。
- [2026-09-23 12:31:10] [2026-09-23 14:40] AttributeOverviewUltra 整体横向滚动实现修正（续 12:23:22 条目）：①Odin/EditorGUILayout.BeginScrollView 内的 GUILayout 组**不响应向视口宽拉伸**——实测 GUILayout.MinWidth(560) 与 MinWidth+ExpandWidth(true) 均不拉伸（表格恒 543，右侧大量空位，用户投诉）；ScrollView 的 available 不传递视口宽，ExpandWidth 无效。②正确方案=Layout 事件缓存宽度 + GUILayout.Width 硬分配：DrawEditor 里 `if (Event.current.type == Layout) _contentWidthCache = Mathf.Max(560, position.width - MenuWidth - 46f)`，46=WindowPadding 左右 15+15+竖滚动条 16（GUIColor 页 scrollPos.y=1477 证实内容普遍超一屏、滚动条常驻；矮面板右缘会有 ~16px 缝隙，已知取舍）；实测表格 779 拉满视口 796 ✓。两遍契约=只 Layout 算、Repaint 复用缓存，拖菜单分隔条（MenuWidth 每帧变）也不会造成两遍不一致。③GUITable.DrawTable 自身是 ExpandWidth().MinWidth(minTableSize+10*auto列数)（反编译 GUITable.cs L228），表格会吃满所在组宽——组不拉伸时表格恒 543 的根因在组不在表。④**docked EditorWindow 绝不能赋值 window.position**——会扰动整个 dock 布局的邻接面板（用户遭遇"窗口分隔条锁死无法拖拽"，我的验证脚本曾对 docked 的 Ultra 窗口赋 position 三次；恢复=Window→Layouts 重载布局）。验证脚本对 docked 窗口只许只读 + window.Repaint/QueuePlayerLoopUpdate，尺寸相关验证交用户手动。⑤Ultra 窗口当前 docked=True、position 属性读出的 (435,194,1050×750) 是 dock pane 虚拟 rect；cell Rect 坐标是窗口局部 GUI 坐标系。
- [2026-09-24 21:45:46] UsageExamples 官方底稿迁移已完成（2026-09-23，7 个 feat(examples) 提交 fedd9e9..b346bbf，109 文件 +4126/−750）：109 个示例 SO 以 Odin 4.0.2.4 反编译 Examples 为底稿重写，融合本地既有用法零删除。验证链=编译 0 错 0 警 + PropertyTree 全量 dump（123 类 0 控制台错误/0 双绘/0 点号组名）+ Ultra 冒烟（70 面板 86 预览项全部内存替换成功）。**修复的系统性坑**：①ActionResolver 不支持 $ 引用（[OnInspectorInit("$xxx")] 等运行时报错），本地此类写法已改为 @表达式引用成员；②空构造 On* 特性只能挂方法不能挂字段；③Odin 组路径父节点必须由某成员组特性直接创建（只写子路径报 "expected a group ... to exist"）；④同成员两个不相关组 ID 会双绘（HideLabelExampleSO 已修）。**既有缺口（非本次引入；2026-09-24 用户拍板维持现状不补建面板，留待后续任务）**：21 个 Data 无面板类（），其 25 个示例类已更新但因无面板不显示；另 10 个孤儿 SO（无 Data 引用）未更新（CustomContextMenu×2、DisableContextMenu、DetailedInfoBox×3、DisableInInlineEditor、ShowInInlineEditor、HideDuplicateReferenceBox、HideInTables、HideMonoScript、HideReferenceObjectPicker）。

- [2026-09-24 21:45:46] AttributeOverviewUltra Phase 3 已完成（2026-09-24，commit 4d37d3a，任务前存档点 888e626）：Attribute Overview Pro 已全量移除（窗口 + AttributeOverviewDatabaseSO + AttributeOverviewDatabase/OdinExamples/UnityExamples.asset），Ultra 独立承担。关键机制：①示例单例 Instance 后端 = UltraStateBankSO.GetMemoryExample&lt;T&gt;()（银行内存路由，key=类型名，89 个 Data 构造器零改动整体切换）；②[OnInspectorInit] 重跑后选中恢复 = UltraPanelDatabase.RestorePanelSelection/PanelSelectionNeedsRestore（窗口 DrawEditor 的 Layout 开头调，Repaint 检测到需恢复只 Repaint() 排队，替代已删除的 PanelNeedsExampleSwap 资产检测 swap 机制）；③LoadOrCreateBank（静态缓存）在 UltraStateBankSO 上，窗口与 ExampleSO 路由共用。路径/菜单变更：数据目录改名 Assets/Editor Default Resources/Aesir Inspector/Attribute Overview（MoveAsset 保 GUID，UltraStateBank 49 条用户状态无损迁移）；AesirInspectorPaths 仅剩 AttributeOverviewDataPath + AttributeOverviewUltraStateBankPath；菜单仅 Attribute Overview Ultra（order -900）。GettingStarted 初始化按钮 = 零资产生成 + 确保银行 + 全量面板构建冒烟。双胞胎清理：保留 Essentials 的 CustomValueDrawerPanelSO、删 Misc 的 CustomValueDrawerAttributePanelSO（Odin 官方 InternalAttributeRegistry 将 CustomValueDrawer 归类 Essentials）。Why: 用户拍板 Ultra 全面取代 Pro；21 个无面板 Data 维持现状不补建（留待后续任务）。How to apply: 新示例/面板一律走银行路由 Instance 惯用法；面板不再是资产单例（AttributeOverviewPanelSO&lt;T&gt; 的 Instance 已删）；Ultra 窗口状态变更仍遵守 Layout 开头铁律。
- [2026-09-24 22:07:20] AttributeOverviewUltra 目录结构复刻 Odin 官方注册表（2026-09-24，commit cc287a8）：新建 Editor/AttributeOverviewUltra/Core/AesirAttributeRegistry.cs = ①69 条面板类型↔Odin 特性类型映射表（PanelToOdinAttribute，唯一名字不同的是 DetailInfoBoxAttributePanelSO↔DetailedInfoBoxAttribute）②分类/显示名/排序直读 Odin 官方公开 API（Sirenix.OdinInspector.Editor.Examples.AttributeExampleUtilities.GetAllOdinAttributes/GetAttributeCategories），零维护随 Odin 升级；显示名 = `attrType.GetNiceName().Replace("Attribute","").SplitPascalCase()`（Sirenix.Utilities 扩展；GUIColor 保持连写）；分类排序复刻官方 CategoryComparer（Essentials -10 / Misc 8 / Meta 9 / Unity 10 / Debug 50，其余 0 字母序），分类内保持官方注册表顺序；多分类由注册表 Categories 逗号分隔（Button/ButtonGroup 在 Groups+Buttons 等 17 条）；未映射面板归 "Aesir Customs"（-20 置顶，字母序）。UltraPanelDatabase 已删除旧 AesirAttributeCategory 枚举分类机制，[AttributeCategory] 标注为已弃用（69 个面板文件上的标注仍在但不参与菜单）。验证方式：打开官方 AttributesExampleWindow 对比两棵树 fullPath（Ultra 非自定义路径 mismatch=0）。**How to apply:** 新增 Odin 特性面板必须在映射表登记否则落入 Aesir Customs；不要用 [AttributeCategory] 判断分类。
- [2026-09-24 23:03:45] AttributeOverviewUltra 分类补齐完成（2026-09-24，commit 496a76a）：Meta / Unity / Debug 三个缺失分类已补齐，映射表 69→76 条，官方 12 个分类全部齐全（验证：打开官方 AttributesExampleWindow 对比 officialOnlyCats 为空、mismatch=0、98 叶子）。新增面板：SuppressInvalidAttributeError（Meta）、ShowDrawerChain + ShowPropertyResolver（Debug，Data/示例原已存在仅缺面板）、Multiline / Range / Space / TextArea（Unity 内置特性在 Odin 下的绘制，Data+示例从零新建；Range 同时属 Unity 与 Validation）。原"21 个无面板 Data"缺口降至 18 个。**How to apply:** 面板↔特性映射必须登记在 AesirAttributeRegistry.PanelToOdinAttribute（按字母序），Unity 内置特性用全限定名（UnityEngine.MultilineAttribute 等）；显示名由官方规则自动得出（如 SuppressInvalidAttributeErrorAttribute → Replace 全局去掉 "Attribute" → "Suppress Invalid Error"，与官方截图一致）。
- [2026-09-24 23:26:09] AttributeOverviewUltra 目录与 Odin 官方完全对等（2026-09-24，commit 57bc1d2）：新增 33 个面板后映射表 76→109 条，Ultra 菜单逐条比对官方 AttributesExampleWindow 达成 missing=0 / extra=0（ultraLeaves=137 = 官方 133 + Aesir Customs 4；113 面板）。补齐明细：17 个仅缺面板（）、1 个缺示例（TableColumnWidth）、8 个缺 Data（Disable|ShowInInlineEditors、Custom|DisableContextMenu、HideDuplicateReferenceBox|HideInTables|HideMonoScript|HideReferenceObjectPicker）、7 个完全新建（ResponsiveButtonGroup/Image/DrawWithUnity/Unit/Toggle/DisallowModificationsIn 示例按官方移植 + HideNetworkBehaviourFields 无示例）。**HideNetworkBehaviourFields 无示例的原因**：本项目 manifest 未启用 com.unity.modules.unet，NetworkBehaviour 不可用。**顺带修复共享缺陷**：AbstractAttributePanelSO 在示例列表为空时不再调用 UpdateExampleCode（AesirInspectorReset 与 Internal_SetData 两处调用点加守卫），此前无示例面板会输出 "[AttributeOverview] attribute 不能为空" 错误。**How to apply:** 新增 Odin 特性面板时若官方注册表有对应特性，必须在 AesirAttributeRegistry 登记才能出现在正确分类；示例为空的面板（如依赖未启用模块的特性）现可安全存在。

### Reference
- [2026-09-23 11:59:02] [2026-09-22] Odin 4.0.2.4 反编译源码：①独立工程 /Users/yuumix/Projects/Unity/OdinInspectorSources/OdinInspectorReverse/01-Decompiled/（Sirenix.OdinInspector.Editor + Sirenix.Utilities.Editor）；②2026-09-23 起本仓库 AesirInspector 内有完整副本 OdinInspectorReverse/01-Decompiled/（8 个程序集目录，含 Sirenix.OdinInspector.Attributes 与 Sirenix.OdinInspector.Editor，官方示例类在 Sirenix/OdinInspector/Editor/Examples/）。AesirInspector 开发中验证 Odin 内部 API 行为（DrawerPriority、SdfIcons 缓存语义、RefreshSetup 是否 Dispose Drawer、LabelTextAttributeDrawer 官方范式等）优先查仓库内副本，比猜测可靠。

